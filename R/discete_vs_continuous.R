# Discrete range

a <- 1:10

# Continuous 
# between any two real numbers there is an infinite number of possible numbers
# - all values between 0 and 1 for example
# You cannot represent a continuous range directly.  You can represent it as a pair though.
# Note there is no standard way to do this.
a <- c(0,1)
b <- list("min"=0, "max"=1)

